apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrepository.yaml
  - helmrelease.yaml
  # Apply clusterissuer after cert-manager CRDs are ready.
  # - clusterissuer.yaml
