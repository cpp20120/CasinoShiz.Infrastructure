apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: casinoshiz-bot2
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: casinoshiz-bot2-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
