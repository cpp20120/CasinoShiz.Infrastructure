all:
  hosts:
    production:
      ansible_host: CHANGE_ME
      ansible_user: root
      ansible_port: 22
  vars:
    # Empty means install the current stable k3s release.
    # Pin before production rollout after validation.
    k3s_version: ""
    k3s_disable: []
    k3s_write_kubeconfig_mode: "0640"
