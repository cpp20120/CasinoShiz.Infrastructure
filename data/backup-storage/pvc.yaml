apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-backups
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 50Gi
